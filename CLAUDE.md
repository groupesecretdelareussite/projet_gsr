# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project layout

This repo's working root for the Next.js app is `gsr/` (this directory) — the parent folder (`PROJET_GSR/`) is not a git repo and mostly contains loose reference images unrelated to the app. All commands below assume you're in `gsr/`.

`GSR_ARCHITECTURE.md` at the repo root (one level up, `PROJET_GSR/GSR_ARCHITECTURE.md`) is the authoritative design reference — database schema, RLS policies, business rules, and the full 20-item "interdits absolus" (absolute prohibitions) list. **Read it before touching auth, permissions, payments, suspension, TD arbitrage, or fin-d'année logic** — a lot of the business logic here is non-obvious and encoded there, not in code comments. It describes the *target* system (v2.1); see "Current implementation state" below for what actually exists in code today.

## Commands

```bash
npm run dev      # start dev server (localhost:3000)
npm run build    # production build
npm run lint     # eslint .
npm run test             # Vitest — Levels 1+2 (unit + role-guard), no network required
npm run test:watch       # Vitest watch mode, Levels 1+2
npm run test:integration # Vitest — Level 3, real network calls against the Supabase project (needs .env.local + .env.test.local)
```

Three-tier test strategy (see `*.test.ts` next to the files they cover, and `*.integration.test.ts` for Level 3): Level 1 pure `src/lib/` unit tests, Level 2 Server Action role-guard tests (`@/lib/auth-scope` mocked, no network), Level 3 integration tests against the real (current) Supabase project — treated as the de facto test environment for now; a client-owned project will be created later and migrated via the same `sql/` files.

Seeding the first admin account (one-shot, service-role key required — never run against prod carelessly):
```bash
SUPABASE_SERVICE_ROLE_KEY=xxx SEED_ADMIN_USERNAME=admin SEED_ADMIN_PASSWORD=xxx npx tsx scripts/seed-admin.ts
```

SQL migrations live in `sql/` and are applied manually to Supabase in numeric order (`001_schema_public.sql` → `023_recompenses.sql` as of this writing); there is no migration runner. **Local test Supabase (`.env.local`): `001`–`020` confirmed applied (2026-08-15, reconciled from `013` in one session — see "Historique et points ouverts"); `021`–`023` not yet.** **Client Supabase: `001`–`019` confirmed applied (2026-08-15); `020`–`023` not yet.** `021`–`023` (2026-08-18) n'ont encore tourné nulle part — appliquer sur test d'abord, vérifier, puis sur client avec `020`.

## Current implementation state

All 7 core phases of `GSR_ARCHITECTURE.md` are now built, plus Portail TD, Comptabilité globale, Notes/Moyennes, et Récompenses. As of now:
- **Vitrine** (public site): `src/app/(vitrine)/*` — complete (actualités, galerie, tarifs, sites, programmes, mentions légales).
- **Admin — élèves module**: login, inscription, liste, modification, suspension/réinscription, deux contacts parents (`src/app/admin/eleves/*`, `src/actions/eleves.ts`).
- **Admin — paiements module**: enregistrement, historique, à-jour, en-retard (filtre par mois, relance WhatsApp journalisée), suppression avec mot de passe+motif, pénalité de réinscription (`src/app/admin/paiements/*`, `src/actions/paiements.ts`, `src/lib/paiements.ts`).
- **Admin — récompenses module** (2026-08-18) : `src/app/admin/recompenses/*`, `src/actions/recompenses.ts`, `src/lib/recompenses.ts`, `sql/023_recompenses.sql`. Suivi mensuel et cumul annuel des récompenses financières d'excellence (Interros sciences = 20/20 -> 200F, Devoirs toutes matières >= 18/20 -> 500F). Paiement unitaire et "Tout payer" reliés directement aux dépenses annexes (catégorie `'Récompense'`). Gardé par rôle (`coordonnateur`/`comptable`/`superviseur`).
- **Export Excel** (2026-08-13) : `src/lib/export-excel.ts` + `src/components/admin/ExporterExcelButton.tsx`, génération client-side (`xlsx`), titre descriptif en ligne 1 de la feuille. Présent sur Élèves > Liste/Suspendus, Paiements > Historique/À jour/En retard, Récompenses > Palmarès/Cumul, Comptabilité — gardé par rôle (`coordonnateur`/`comptable`/`superviseur`, `coordonnateur`/`comptable` seul pour Comptabilité). Exclus volontairement : Notes/Moyennes, Paiements > Supprimés, Statistiques.
- **Suspension automatique** (§12.3, 2026-07-28) : `src/lib/suspension-auto.ts` (logique) + `src/app/api/cron/suspension-auto/route.ts` (déclenchée par le cron Vercel défini dans `vercel.json`, `0 0 1 * *`). Authentifiée par secret partagé `CRON_SECRET` (env var — même valeur requise côté Vercel une fois déployé), pas par `getUserScope` (aucune session admin dans ce contexte). Pour tester manuellement avec une date simulée : `SIMULE_DATE=2026-11-01 npx tsx scripts/test-suspension-auto.ts` — ce n'est pas un dry-run, ça suspend réellement les élèves en défaut.
- **Admin — paramètres**: gestion utilisateurs (`src/actions/utilisateurs.ts`), coefficients (`src/actions/coefficients.ts`), données scolaires — sites/classes/frais TD (`src/actions/donnees-scolaires.ts`), tous coordonnateur-only.
- **Admin — tableau de bord**: KPIs, répartition par classe (recharts), alertes de retard (`src/app/admin/tableau-de-bord/page.tsx`).
- **Admin — présences, notes/moyennes, comptabilité (dépenses annexes), fin d'année, notifications, galerie, actualités**: each has its own `src/app/admin/<module>/*` + `src/actions/<module>.ts` + tests.
- **Portail parents** (`src/app/portail-parents/*`, `src/actions/auth-parent.ts`, `src/lib/session-parent.ts`): création de compte, dashboard.
- **Portail TD** (`src/app/td/*`, schema `td` in `sql/002_schema_td.sql` + `009_td_module.sql`+): `td/coord/{dashboard,planning,arbitrage,config,finance}` for the coordonnateur (reuses the admin session — see auth model below), `td/prof/{dashboard,candidatures,aide}` for professeurs (`src/actions/auth-td.ts`, `src/lib/session-td.ts`). Weekly schedule state lives in `src/actions/td-semaines.ts` — a "semaine" (Monday–Sunday, any day allowed for its creneaux) has creneaux and a publication state the coordonnateur controls from `td/coord/planning`. `td.creneaux.classe_id` references `public.classes` directly (not an independent TD-only list — see `sql/015_td_classes_reelles.sql`), which is what makes the public vitrine page possible: `src/app/(vitrine)/programmes/page.tsx` is a Server Component reading the currently `publiee` semaine's creneaux through the normal RLS-bound client (`anon`-scoped read policies in `sql/017_programmes_publics_rls.sql`), grouped/filterable by real site.
- When extending any module, treat `GSR_ARCHITECTURE.md` as the spec of record but verify current code state first — don't assume a table, RLS policy, or Server Action described there already exists without checking `sql/` and `src/actions/`.
- `classes.classe_suivante_id` models a simple 1:1 promotion chain only. Where a grade level is a genuine decision point (e.g. a "3ème" splitting into multiple séries at "Seconde"), leave it `NULL` and say so in a comment — see `sql/005_seed.sql`'s Jéricho block for the pattern. A future fin-d'année Server Action must treat `NULL` as "needs manual per-student review" (pick a `decisions_passage.nouvelle_classe_id`, possibly cross-site), not auto-assume `sortant` — that assumption only holds for a true end-of-chain level.
- `npm run build` and `npm run lint` are both clean as of this writing — no known build-breaking issues in the current tree.

## Historique et points ouverts

Repères pour ne pas reperdre le contexte d'une session à l'autre. Le détail fin de chaque changement reste dans `git log` — cette section ne garde que les décisions, leur "pourquoi", et ce qui reste en suspens.

### En attente / à ne pas oublier
- **`src/app/admin/eleves/suspendus/page.tsx` reste sans filtrage par site pour un superviseur** — voit les suspendus de tous ses sites sans distinction (mineur, pas signalé comme gênant). Le contrôle de rôle sur les *actions* (Réinscrire/Exporter, `coordonnateur`/`comptable`/`superviseur` uniquement) est en place depuis le 2026-08-16 ; la page elle-même reste volontairement ouverte aux 5 rôles (chef_site/secretaire peuvent consulter, décision explicite du 2026-08-16, voir "Repères de fond").
- **Paiements > Enregistrer/Supprimés restent fermés à `chef_site`** (redirigé vers Historique s'il clique "Paiements" dans le menu) et entièrement à `secretaire` — voir "Repères de fond" 2026-08-16 pour le détail complet du nouveau périmètre par page.
- **Mentions légales publiées avec un IFU volontairement fictif** (`src/app/(vitrine)/mentions-legales/page.tsx`, entreprise pas encore enregistrée) — encart d'avertissement visible sur la page, mais à corriger avant toute communication officielle. Adresse laissée vague ("Cotonou, Bénin", pas de siège social). Politique de confidentialité détaillée : pas décidée, pas rédigée.
- **Gel du portail TD pendant les vacances** : idée proposée et analysée en détail (bouton de gel, page Programmes affichant un message vacances) — le client a explicitement décidé de ne pas y donner suite (2026-08-13). Ne pas reproposer sans nouvelle demande.
- **3 fichiers vitrine annoncent encore un horaire TD obsolète** ("Mercredi · Samedi · Dimanche", alors que la règle est "n'importe quel jour de la semaine" depuis fin juillet 2026) : `src/components/vitrine/BrochurePDF.tsx`, `src/app/(vitrine)/sites/page.tsx`, `src/components/vitrine/HomeClient.tsx`. Repéré plusieurs fois, jamais corrigé — en attente d'une décision utilisateur.
- **Restriction des candidatures professeur par zone** : explicitement laissée de côté au moment de lier `td.creneaux` aux vraies classes — rien n'empêche aujourd'hui un professeur de postuler hors de sa zone d'affectation.
- **`td.zones` et `public.sites` restent deux tables séparées** bien qu'elles se recouvrent (mêmes noms de sites) — pas unifiées, à reconsidérer si ça devient gênant.
- **`public.matieres` et `td.matieres_td` sont deux tables indépendantes**, même précédent que `td.zones`/`public.sites`. `public.matieres` (10 matières, `sql/005_seed.sql`) est une liste fixe seedée une fois — aucun écran admin pour en ajouter/modifier, seulement lue (coefficients, notes). `td.matieres_td` a les mêmes 10 noms au départ mais est librement gérée par le coordonnateur (`creerMatiereTD`/`modifierMatiereTD`/`supprimerMatiereTD`, `/td/coord/config/matieres`) — les deux listes peuvent diverger sans avertissement.
- **"Agent IA"** : item de menu grisé, scope jamais défini — ne pas construire sans reconfirmer avec l'utilisateur.
- **Galerie vitrine publique** : contenu statique, pas branchée sur Supabase Storage — différé volontairement, à ne pas confondre avec la Galerie admin interne (`/admin/galerie`), elle terminée.
- **4 documents livrables attendus par le client** (document client, guide utilisateur, contrat de maintenance, document ultra-technique pour un futur développeur humain) — pas commencés ; leur condition de déclenchement d'origine (migration Next 16 faite + infra dédiée client en place) est remplie.
- **Comptes `secretaire` déjà existants sans `site_id`** : une fois `sql/020` appliquée, chaque compte secretaire existant doit être édité manuellement (Paramètres > Utilisateurs) pour lui assigner un site — sans ça `getUserScope()` lui donne un périmètre vide (`siteIds = []`, aucune erreur, juste rien à afficher tant que le site n'est pas assigné).

### Infra de déploiement — état réel (source de confusion récurrente, à ne pas réinterpréter)
- GitHub du projet : `groupesecretdelareussite/projet_gsr` (organisation du client — le transfert depuis le compte personnel du développeur est fait).
- Vercel + Supabase dédiés au client : déjà en place et fonctionnels d'après l'utilisateur — l'assistant n'y a pas accès et n'a pas besoin de s'y connecter pour travailler.
- `.env.local` de ce dépôt local pointe **volontairement** sur un projet Supabase de dev/test séparé (données de démo, `college = 'COLLEGE DEMO'`) — ce n'est **pas** un signe que l'infra client manque ; ne pas réinterpréter cet état comme une anomalie à corriger.
- L'ancien "gel" de push (posé quand Vercel pointait encore sur ce même Supabase de test) n'a jamais été explicitement levé mais sa justification semble résolue — appliquer simplement la règle standard du projet : jamais de `git push` sans un "commit et push" explicite de l'utilisateur, à chaque fois, sans le traiter comme un gel spécial actif.

### Repères de fond
- Les 7 phases de `GSR_ARCHITECTURE.md` ont été bouclées, puis Portail TD, Comptabilité globale et Notes/Moyennes construits dans la foulée (détail dans "Current implementation state" ci-dessus).
- Le client a formulé ses exigences de livraison finale (infra dédiée, preview de recette avant prod, 4 documents livrables) après cette première vague de fonctionnalités.
- Un audit sécurité externe a été traité : headers HTTP (CSP statique sans nonce, HSTS, X-Frame-Options, nosniff), rate-limiting sur la création de compte parent.
- La migration Next.js 14→16 (API serveur asynchrones, `middleware.ts`→`proxy.ts`, React 19, ESLint flat config) est terminée — confirmé via `next: 16.3.0` dans `package.json`.
- Deux contacts parents ajoutés (WhatsApp obligatoire pour au moins un des deux, format Bénin normalisé `+229 01 XX XX XX XX`).
- Règle TD "semaine" assouplie : lundi→dimanche, n'importe quel jour autorisé pour un créneau (avant : Mercredi/Samedi/Dimanche uniquement).
- `td.creneaux.classe_id` migré de la liste indépendante `td.classes_td` vers les vraies classes de l'école (`public.classes` — site + section réels, ex. "Terminale A" à Jéricho, pas juste "6ème" générique) : ça débloque un programme TD vraiment scopé par site, et corrige un bug où deux vraies sections de même niveau au même site ne pouvaient pas avoir de créneau à la même heure. Bug préexistant corrigé au passage dans `td.arbitrer_creneau()` (Règle A) : ne détectait qu'une égalité stricte d'heure de début, pas un vrai chevauchement d'intervalles horaires.
- **Bug corrigé 2026-08-11 — inscription impossible avec un seul contact parent** : `PhoneInput` (react-international-phone) déclenche son `onChange` avec l'indicatif seul (ex. `+229`) dès qu'un champ contact reste vide au montage ou qu'on change de pays dans son sélecteur, sans qu'aucun chiffre n'ait été saisi. Le champ "resté vide" à l'écran n'était donc pas vraiment vide côté état React, et se faisait rejeter par la validation — alors que la règle voulue est qu'un seul des deux contacts suffit. `normaliserNumero()` (`src/lib/telephone.ts`) neutralise maintenant ce cas.
- **CSP `img-src` élargie à `cdnjs.cloudflare.com`** (2026-08-11, `next.config.mjs`) : les drapeaux de `PhoneInput` chargent leurs SVG Twemoji depuis ce CDN — bloqués depuis l'audit sécurité du 10/08 car dépendance transitive non repérée par le grep initial (image cassée à l'écran, sur n'importe quel navigateur, rien à voir avec les polices emoji du système).
- **Sauvegarde quotidienne** (`.github/workflows/sauvegarde-quotidienne.yml`, cron 3h Bénin, dump `public`+`td` vers la branche `backups`) : cassée du 9 au 13 août 2026 (secret `DATABASE_URL` mal formé — mot de passe avec un `@` non encodé en `%40`), puis un deuxième blocage une fois l'encodage corrigé (connexion directe Supabase en IPv6 seul, injoignable depuis les runners GitHub Actions — il faut l'URL du **connection pooler**, pas la connexion directe). Le job de vérification (restauration dans un conteneur Postgres jetable) stub aussi désormais les rôles `anon`/`authenticated`/`service_role` (NOLOGIN), nécessaires depuis que des policies RLS (ex. `sql/017`) les ciblent explicitement par nom.
- **`libererCreneauTD()`** (`src/actions/td-creneaux.ts`, `sql/018`, 2026-08-13) : le coordonnateur seul (pas de self-service côté prof) peut libérer un créneau clôturé si le professeur validé se désiste — nouveau statut de postulation `Retiree` (distinct de `Refuse`), ne remet en `En attente` que les candidats initiaux encore réellement libres (pas ceux validés depuis sur un créneau qui chevauche). `td.arbitrer_creneau()` a gagné un garde-fou anti-double-booking indépendant de cette fonctionnalité (refuse de valider un professeur déjà validé ailleurs sur un créneau chevauchant).
- **Notes par semestre + Suivi Moyennes** (`sql/019`, 2026-08-13) : `notes` gagne une colonne `semestre` (S1/S2 cohabitent en base, rien n'est jamais supprimé — pas de bouton de purge). `moyennes_semestrielles` est un instantané figé par élève/semestre, écrit uniquement par `cloturerSemestre()` (coordonnateur, rejouable). Page `/admin/notes/suivi-moyennes` : MA = (MS1 + MS2×2) / 3.
- **Audit global de l'app (2026-08-14)** : complétude, cohérence UI/UX, responsivité et pistes de fonctionnalités — livré comme document séparé (artifact), pas dans ce fichier. A révélé au passage que la doc du choix de police (ci-dessus) ne correspondait plus au code.
- **Police unifiée sur Lexend (2026-08-15)** : tout le site (admin, TD, portail parents, vitrine) charge désormais Lexend une seule fois (`src/app/layout.tsx`) — avant, Inter sitewide par défaut avec un override Lexend isolé sur la vitrine (`(vitrine)/layout.tsx`).
- **Historique des présences + Retardataires (2026-08-15)** : conçu à partir de 3 fichiers PHP de l'ancien système (`historique_presence.php`, `presence_add.php`, `presence_td.php`) fournis par le client, réécrits from scratch avec les conventions actuelles (aucun code porté tel quel — l'ancien avait deux requêtes SQL non préparées). `PresencesNav` (nouveau, calqué sur `PaiementsNav`) ajoute 2 onglets à Présences : *Retardataires* (`/admin/presences/retardataires`, `marquerRetardataire()` dans `src/actions/presences.ts` — résout classe/site/année en cours côté serveur à partir du seul `eleveId`, protégé par RLS `presences_acces`) et *Historique* (deux routes séparées, pas un switch interne façon PHP — cohérent avec le pattern déjà en place pour Paiements) : `historique/classe` (site+classe+date → tableau + 4 `KpiCard` Total/Présents/Absents/Taux + double export Excel Présents/Absents, comme l'ancien système) et `historique/eleve` (autocomplete + mois scolaire → Date/Statut). Nouveau composant partagé `EleveAutocomplete` (extrait du pattern déjà dans `EnregistrerPaiementForm.tsx`, branché sur `/api/eleves-recherche` existant) — `EnregistrerPaiementForm`/`Topbar` non retouchés, toujours leur propre copie. Nouveau helper `src/lib/presences.ts` (`plageDatesMoisScolaire`) : `presences.date_presence` n'a pas de colonne "mois" contrairement à `paiements.mois_souscription`, donc filtrer par mois scolaire suppose de reconstruire la vraie plage de dates à partir de l'année scolaire en cours (Octobre-Décembre restent sur l'année de `dateDebut`, Janvier-Mai basculent sur l'année suivante). `<input type="date">` du mode Classe volontairement libre (pas de restriction aux seules dates avec présence enregistrée — HTML natif ne sait pas faire une liste discrète de dates autorisées, seulement `min`/`max` continus ; une vraie restriction demanderait un calendrier JS personnalisé, jugé disproportionné) — état vide `EmptyState` si la date choisie n'a rien. Périmètre : les 5 rôles habituels de Présences (l'ancien système excluait `chef_site`/`superviseur`, restreint à `coordonnateur`/`comptable`/`secretaire` — le client a choisi d'aligner sur le périmètre actuel plutôt que de reproduire cette restriction). **Nav retouchée le lendemain** : les 3 onglets `PresencesNav` restent toujours sur une seule ligne (pas de `flex-wrap` — un menu qui casse sur deux lignes n'est pas acceptable), icône systématique, libellé masqué sous `sm:` ; actif = soulignement (`border-b-2`), plus un fond plein. Les 2 sous-onglets `HistoriqueModeTabs` gardent le même style de soulignement mais, choix volontaire distinct, affichent toujours leur libellé quel que soit l'écran (pas de `hidden sm:` — labels assez courts pour ne pas avoir besoin de se réduire à l'icône seule).
- **`secretaire` aligné sur `chef_site` (2026-08-15, `sql/020`)** : `GSR_ARCHITECTURE.md` documentait déjà ce rôle comme "droits en attente de décision" depuis la v2.0 (lignes 8 et 799 du doc) — le client a tranché : mêmes droits que `chef_site`, pas un nouveau profil à inventer. Touche `trg_users_scope_coherence` (secretaire peut désormais avoir un `site_id`, comme chef_site), 5 policies RLS (`eleves`/`presences`/`notes`/`notifications`/`moyennes_semestrielles`), `auth-scope.ts` (calcul de `siteIds`), et une vingtaine de gardes de rôle côté TS (Server Actions `notes.ts`/`presences.ts`/`utilisateurs.ts`, pages Élèves/Notes/Présences/Paiements, `admin-nav.ts`, dialogues de création/modification utilisateur). Paiements, comptabilité, exports, contacts parents, etc. restent hors de portée pour les deux rôles, sans changement. **Correction en cours de route** : la policy `moyennes_semestrielles` a d'abord été ajoutée en éditant `sql/019` sur place (supposée non appliquée) — faux, le client l'avait déjà jouée telle quelle (`019` confirmé jusque-là côté client, découvert juste après) ; déplacée dans `020`, seul fichier garanti pas encore exécuté nulle part, `019` restauré à son contenu réellement live sur le client.
- **Nouvel ajustement du périmètre chef_site/secretaire (2026-08-16, `sql/021`)** — le client a demandé 5 changements ponctuels, chacun documenté ici car ils cassent par endroits la symétrie stricte chef_site=secretaire posée le 15 :
  - **Dashboard** : le KPI "Non à jour" s'ouvre à chef_site ET secretaire (`peutVoirKpiNonAJour`) ; le panneau d'alerte retard (lien direct vers Paiements > En retard) reste borné à qui peut réellement y accéder — chef_site seul, pas secretaire (`peutVoirAlertePaiements`, `tableau-de-bord/page.tsx`).
  - **Élèves suspendus** : page ouverte aux 5 rôles (le bouton sur Élèves > Liste n'a jamais eu de garde et le reste), mais Réinscrire et Exporter restent `coordonnateur`/`comptable`/`superviseur` uniquement (`peutGerer`).
  - **Paiements** : Historique/À jour/En retard s'ouvrent à `chef_site` en lecture seule (pas de boutons d'action, pas d'export) — **pas secretaire**, seule vraie asymétrie entre les deux rôles à ce jour. Enregistrer et Supprimés restent fermés à chef_site ; `paiements/enregistrer/page.tsx` redirige chef_site vers Historique (seul `href` possible dans `admin-nav.ts` pour l'entrée de menu).
  - **Notifications** : ouvertes à chef_site ET secretaire — la policy RLS `notifications_acces` (sql/020) les couvrait déjà, il ne manquait que la garde de page et l'entrée `admin-nav.ts` ; aucun filtre `site_id` applicatif nécessaire, RLS scope tout seul.
  - **Galerie admin** : ouverte à chef_site ET secretaire, lecture/ajout complets, suppression bornée à ses propres photos. Contrairement à Notifications, la RLS de sql/008 excluait ces deux rôles *explicitement* ("pas chef_site, pas secretaire") — nouvelle migration `sql/021` scindant l'ancienne policy `FOR ALL` unique en 3 (lecture/ajout ouverts, suppression conditionnelle sur `ajoute_par = auth.uid()` pour ces deux rôles) côté table `galerie_admin` ET côté `storage.objects` (bucket `galerie-admin`, condition sur la colonne `owner` que Supabase Storage remplit automatiquement à l'upload). Contrôle applicatif redondant dans `supprimerPhotoGalerieAdmin` (message clair plutôt qu'un échec RLS opaque).
  - **Mon compte** : bouton de déconnexion ajouté (manquait totalement — seul moyen de se déconnecter jusqu'ici était la Sidebar), même pattern que `Sidebar.tsx` (`logout()` puis redirection `/admin/login`).
- **Audit UI/UX (2026-08-14) — Priorité 1 (contrôle de rôle) close le 2026-08-16** : des 4 points, 3 étaient déjà réglés par des changements ultérieurs (Paiements par la refonte de rôle ci-dessus, "Élèves suspendus" par la décision explicite du client d'ouvrir la page aux 5 rôles, `eleves/liste` par l'alignement secretaire=chef_site du 15). Restait `eleves/[id]/modifier/page.tsx` : n'appelait pas du tout `getUserScope()` — n'importe quel compte connecté pouvait consulter/modifier le contact parent d'un élève par URL directe. Garde ajoutée (`coordonnateur`/`comptable`/`superviseur`, même périmètre que `peutGerer`). Restent en Priorité 2/3 de l'audit (cohérence UI/UX 🟠🟡🟢, responsivité, contenu vitrine obsolète) — voir l'artifact "Audit GSR" pour le détail complet, pas dupliqué ici.
- **Audit UI/UX — Priorité 2 (🟠 deux mécanismes pour une même intention / composants partagés non réutilisés) close le 2026-08-16**, 8 constats :
  - Bouton de création "Nouvel article" (Actualités) déplacé d'un `ActionsBar` séparé vers `PageHeader.actions`, seule page à diverger sur ce point — `ActionsBar` reste utilisé pour de vraies actions *secondaires* (ex. "Élèves suspendus" + Export sur `eleves/liste`, cas différent du constat).
  - Trois styles de bouton "créer" unifiés sur `variant="outline" size="sm"` (déjà majoritaire — `NouveauSiteDialog`/`NouvelleClasseDialog`/`AjouterCategorieDialog`) : `NouvelUtilisateurDialog` (vert plein grande taille) et `NouvelleDepenseDialog` (vert plein petit) alignés dessus.
  - Export Excel de Paiements > À jour/En retard déplacé du `<div>` ad hoc sous la nav vers `PageHeader.actions`, comme Historique/Liste/Suspendus ; profité du passage pour convertir le filtre "Mois" d'En retard (bouton "Filtrer" à cliquer) en `AutoSubmitOnChange`, seule page de filtres restée sur l'ancien pattern.
  - `HOVER_ONLY_LABEL` retiré de `ExporterExcelButton` — son usage documenté (`lib/utils.ts`) est pour les actions de ligne dans un tableau (`ACTIONS_HOVER_REVEAL`), pas pour un bouton autonome de bandeau ; le texte est maintenant toujours visible, comme `ToutMarquerLuButton` et "Élèves suspendus" à côté.
  - `PageHeader` gagne un prop optionnel `decorativeIcon` (grande icône en filigrane bas-droite) — le Tableau de bord recréait le bandeau à la main uniquement pour ça ; réutilise maintenant le composant partagé.
  - `DataTable` gagne un prop optionnel `bare` (masque la carte englobante, pour un usage imbriqué dans une carte existante) — les tableaux Notes/Paiements de la fiche élève (`eleves/[id]/page.tsx`) étaient deux `<table>` faites main, remplacées par `DataTable bare` (bascule carte mobile incluse, corrige au passage un manque de responsivité signalé en section 3 de l'audit).
  - `PresencesCards.tsx` : export xlsx maison (`XLSX` importé directement, pas de ligne de titre) remplacé par `ExporterExcelButton`/`export-excel.ts` ; boutons "Tout présent"/"Tout absent" (`<button>` bruts, `font-medium`) convertis en `Button` partagé (`variant="outline" size="sm"`, classes de couleur en surcharge) — alignés en `font-semibold` comme partout ailleurs. **Superseded le même jour, voir "Appel du jour — sauvegarde différée" ci-dessous** : l'export a de nouveau disparu de cette page (retiré du scénario, pas juste déplacé).
  - Téléphone professeur TD (`NouveauProfesseurDialog`/`ModifierProfesseurDialog`) : `<Input>` texte libre remplacé par `PhoneInput` (même composant que les contacts parents) — aucun changement côté `td-config.ts`, le champ reste une simple chaîne stockée telle quelle.
  - Non traité ici (délibérément) : Priorité 3 de l'audit (🟡 messages/dates/interactions divergents, 🟢 bandeaux vitrine incohérents, section 3 responsivité restante, contenu vitrine obsolète) — voir l'artifact "Audit GSR".
- **Audit UI/UX — Priorité 3, bloc 1/5 (🟡 back-office, messages divergents) close le 2026-08-16** : Priorité 3 découpée en 5 blocs (messages back-office ; responsivité back-office ; portails Parents/TD ; vitrine éléments inertes ; vitrine contenu à corriger — ce dernier en attente de décisions client sur les vraies valeurs). Bloc 1, 2 constats :
  - Message de refus d'accès d'Actualités (seule page mono-rôle coordonnateur à diverger) aligné sur "Cette page est réservée au coordonnateur.", comme Fin d'année/Utilisateurs/Coefficients/Données scolaires — la formule "ne vous est pas accessible" reste réservée aux pages à garde multi-rôles.
  - Dates ISO brutes formatées en `fr-FR` : colonne "Date" de Paiements > Historique (écran ET export Excel, par cohérence avec En retard qui formatait déjà sa colonne "Dernière relance") et colonne "Date du paiement" de Paiements > Supprimés (pas d'export sur cette page).
- **Audit UI/UX — Priorité 3, bloc 2/5 (responsivité back-office) close le 2026-08-16**, 6 constats :
  - Panneau de `NotificationBadge` : `w-80` ancré `right-0` (débordait à gauche de l'écran sur mobile car le bouton cloche n'est jamais au bord — Settings/Aide/Mon compte le suivent dans la Topbar). Devient `fixed inset-x-4 top-16` sur mobile (overlay pleine largeur sous la Topbar, 16px de marge), reprend `sm:absolute sm:right-0 sm:top-11 sm:w-80` (comportement inchangé) à partir de `sm:`.
  - `Topbar.tsx` : les deux conteneurs de recherche élève (actif et placeholder désactivé) gagnent `min-w-0` — sans ça, un `flex-1` ne peut pas rétrécir sous la largeur intrinsèque de son contenu, ce qui pousse le reste de la barre hors écran. Le `<select>` de `ToggleSiteSuperviseur` (rôle superviseur uniquement, aggravait le plus ce débordement) gagne `w-28 sm:w-auto shrink-0`.
  - Grilles de filtres `eleves/liste` et `paiements/historique` : `grid-cols-2 sm:grid-cols-4` → `grid-cols-1 sm:grid-cols-4`, comme les 3 pages Notes ; règle au passage l'aggravation citée par l'audit (select "classe" tassé dans ~150px à 2 colonnes).
  - Grille d'images du formulaire Actualités (`ActualiteForm.tsx`) : `grid-cols-3` → `grid-cols-2` sur mobile — à `h-24` fixe, 3 colonnes sur 375px donnait des vignettes très étroites et non carrées ; aligné sur le pattern `GalerieAdminGrid` (2 colonnes en dessous de `sm:`).
  - Troncature `title` (HTML, ne se déclenche pas au toucher) du champ "Motif" sur `eleves/suspendus` et `paiements/supprimes` : `max-w-[220px] truncate` devient `md:max-w-[220px] md:truncate` — texte complet visible sur la bascule carte mobile de `DataTable`, troncature conservée uniquement sur la vue tableau desktop où `title` fonctionne (survol souris).
  - Colonnes `DataTable` sans libellé (`label: ""`) sur Actualités ("Couverture") et Notifications ("Statut") — corrigées, évite une ligne à étiquette invisible dans la bascule carte mobile.
- **Audit UI/UX — Priorité 3, bloc 3/5 (portails Parents/TD) close le 2026-08-16**, 4 constats :
  - Page Aide du professeur TD : le bloc partagé `AIDE_CONNEXION_SECURITE` (`lib/aide-contenu.tsx`) affirmait un lien de réinitialisation par email — vrai pour l'admin/coordonnateur (Supabase Auth), **faux pour le professeur TD** (session bcrypt/iron-session séparée, `connexionProfesseurTD`, cf. `td/login/page.tsx` qui affiche la vraie procédure : "contactez le coordonnateur"). Nouveau bloc dédié `AIDE_CONNEXION_SECURITE_PROF_TD`, utilisé uniquement par `td/prof/aide/page.tsx` ; `admin/aide/page.tsx` (coordonnateur inclus, authentifié comme l'admin) garde l'original, inchangé.
  - Lien "Aide" des deux en-têtes du portail TD (`td/coord/layout.tsx`, `td/prof/layout.tsx`) : icône nue sans zone tactile → même traitement que le bouton Déconnexion juste à côté (`p-2.5 -m-2.5 sm:p-0 sm:m-0`, padding compensé par une marge négative pour ne pas décaler le layout).
  - Bouton afficher/masquer mot de passe du login TD (`td/login/page.tsx`) : `p-2.5` ajouté (position ajustée `right-3.5`→`right-2` en conséquence), aligné sur le même composant du portail parents qui l'avait déjà.
  - Colonne "Contact" des professeurs TD (`td/coord/config/professeurs/page.tsx`) : email + téléphone concaténés sur une ligne sans césure → deux lignes séparées (`block break-words`), email et téléphone lisibles séparément en plus de régler le risque de débordement sur la bascule carte mobile.
- **Présences > Appel du jour — sauvegarde différée (2026-08-16)**, refonte demandée par le client indépendamment de l'audit : l'ancien comportement (chaque coche déclenchait un `upsertPresence` immédiat, élève non touché = aucune ligne en base malgré l'affichage "présent") a été remplacé.
  - Chargement de la page : tous les élèves de la classe choisie s'affichent cochés "présent" — **purement visuel, rien n'est écrit en base à ce stade**. On décoche les absents à volonté (état 100% local, plus aucun appel serveur par carte). "Tout présent"/"Tout absent" sont eux aussi devenus des opérations locales pures (avant : un upsert multi-lignes immédiat).
  - Nouveau bouton "Sauvegarder" : un seul appel serveur (`enregistrerAppelDuJour`, remplace `upsertPresence` + `marquerTousPresence` — tous les deux supprimés, plus aucun appelant) qui upsert l'état exact affiché (présents ET absents, mélangés) en une fois. Succès → toast + `router.push("/admin/presences")` (retour à l'écran de sélection vide, pas juste un refresh).
  - Champ date retiré du formulaire — l'appel du jour ne concerne plus que la date du jour (`aujourdhui()`), aucun choix possible. La saisie d'une date passée n'est plus supportée par cet écran (reste possible ailleurs via Historique/Retardataires si besoin).
  - Bouton Export retiré de cette page (n'avait plus de sens une fois l'état "non sauvegardé" — exporter un brouillon jamais écrit en base aurait été trompeur).
  - Le fetch `presencesExistantes` (pré-remplissage depuis la base) a été supprimé plutôt que conservé : la page repart toujours de "tout présent" à chaque chargement, cohérent avec le retour systématique à `/admin/presences` après sauvegarde — pas de scénario de reprise à gérer.
  - **Un appel sauvegardé est définitif pour la classe/le jour donné** (précision client immédiate après la première version) : `page.tsx` vérifie l'existence de lignes `presences` pour `(classe_id, date_presence)` avant d'afficher `PresencesCards` — si trouvées, affiche un `EmptyState` "Appel déjà effectué aujourd'hui" avec un lien vers Retardataires (`EmptyState` gagne au passage un prop optionnel `action` pour ce cas). Même garde côté serveur dans `enregistrerAppelDuJour` (défense en profondeur — double-onglet ou resoumission ne peuvent pas écraser silencieusement l'appel). Seule modification possible ensuite : Retardataires (`marquerRetardataire`, inchangé), qui passe un élève ponctuel d'absent à présent sans repasser par cette action.
- **Diagnostic de l'état réel des migrations sur `.env.local` (2026-08-15)** : pas de table de suivi des migrations dans ce projet, donc pour savoir où s'était arrêtée la base de test, sondage direct — un script jetable (jamais commité), `@supabase/supabase-js` + clé service role déjà dans `.env.local`, teste par migration l'existence d'une colonne/table caractéristique (`.select(colonne).limit(1)`, erreur = absente) ; pour `017` (policies RLS `anon`), test avec la clé anon plutôt que service role, qui bypass RLS et ne prouverait rien. Aucune valeur du `.env.local` jamais affichée. Fonctions/triggers/grants purs sans colonne nouvelle (`003`, `007`, `016`, `020`) ne sont pas vérifiables par cette méthode.
- **Présences > Historique classe — gestion des dates sans appel (2026-08-18)** : lorsqu'aucun appel n'avait été enregistré pour une date (0 ligne en base), la page affichait tous les élèves comme "Absents" (0% de présence) à cause du fallback `?? false`. Corrigé avec `appelEffectue` pour afficher un `EmptyState` explicite ("Aucun appel enregistré pour cette date").
- **Statistiques & Comptabilité — abréviation des mois sur l'axe X (2026-08-18)** : `EncaissementsMensuelsChart` et `EntreesSortiesChart` utilisent désormais un `tickFormatter` pour afficher les mois abrégés (Oct, Nov, Déc, Jan, Fév, Mars, Avr, Mai) sur l'axe X tout en préservant le nom complet dans l'infobulle (Tooltip). "Mars" est conservé intact sans coupure.
- **Traçabilité des paiements — agent encaisseur (2026-08-18, `sql/022`)** : ajout de `enregistre_par uuid references public.users(id)` sur `public.paiements`, renseigné lors de `enregistrerPaiement` et de la régularisation en réinscription. Affiché dans le tableau de l'Historique des paiements (`Encaissé par`), exclu volontairement des exports Excel. Policy `users_lecture_staff` ajoutée sur `public.users` pour permettre la lecture du nom d'utilisateur par le staff actif.

## Architecture

**Stack**: Next.js 16 (App Router) + React 19 + Supabase (Postgres + Auth + Storage) + Tailwind + shadcn/ui-style components, deployed on Vercel.

### Two Postgres schemas in one Supabase project
`public` holds all GSR data (élèves, paiements, users, sites, classes…); `td` holds the separate TD (professeur scheduling) portal data — though `td.creneaux.classe_id` is a cross-schema FK straight into `public.classes` (see "Historique" above), so "never join across schemas" below is about Supabase-JS query joins specifically, not SQL-level FK constraints. **Never join across schemas in a single Supabase JS query** — issue two separate queries and combine in TypeScript (this is absolute prohibition #8 in the architecture doc).

### Auth model — three independent systems, all built
1. **Admin staff** (`public.users`): Supabase Auth (JWT, httpOnly cookies). The login screen asks for `username`, but Supabase Auth only knows `email` — `login()` in `src/actions/auth.ts` resolves `username → email` via the service-role client first, *then* calls `signInWithPassword`. This lookup can't be skipped or replaced by a direct Supabase Auth call.
2. **Parents** (`public.comptes_parents`): bcrypt + signed httpOnly cookie (`src/lib/session-parent.ts`), no Supabase Auth.
3. **TD professeurs** (`td.professeurs`): same custom bcrypt pattern as parents (`src/lib/session-td.ts`); the coordonnateur reuses their existing admin session inside the TD portal instead of a fourth auth system.

All three are meant to share a 1200s (20 min) inactivity timeout and a brute-force guard via `public.login_attempts` (5 failed attempts / 15 min per identifiant+portail) — see `tropDeTentatives()` in `src/actions/auth.ts` for the pattern already implemented for the admin portal.

### Two Supabase clients with very different guarantees
- `src/lib/supabase/server.ts` / `client.ts` — anon key, **RLS enforced** by Postgres. Default choice for reads.
- `src/lib/supabase/admin.ts` (`createServiceRoleClient()`) — **bypasses RLS entirely**. Only ever imported from `src/actions/*.ts` or `src/app/api/*` route handlers (server-only). Every Server Action that uses it **must call `getUserScope()` before any read/write** — there is no RLS safety net if that check is skipped or misplaced after a query.

### Role & scope model (`src/lib/auth-scope.ts`, `src/lib/constants.ts`)
Five roles: `coordonnateur`, `comptable` (both global scope), `superviseur` (multi-site via `public.user_sites` join table), `chef_site` and `secretaire` (both single-site via `users.site_id`). **`secretaire` has had the exact same rights as `chef_site` since 2026-08-15** (client decision — see "Historique et points ouverts"); before that it was dashboard-only. `getUserScope(supabase)` returns `{ userId, role, siteId, siteIds, isGlobal }`; `siteInScope(scope, siteId)` is the standard guard. Every Server Action that touches scoped data follows the same shape: resolve scope → assert role is allowed → assert the target site is in scope → only then read/write. See `src/actions/eleves.ts` for the reference pattern (`getScopeAndAssert()` + `siteInScope()` before every mutation).

Client-side, `useUserScope()` (`src/hooks/useUserScope.ts`) reads scope from `ScopeProvider` context (`src/components/admin/ScopeProvider.tsx`) — used to conditionally render UI, but this is UI polish only, never a security boundary; the real enforcement is RLS + the Server Action checks above.

### Key business-rule invariants (see `GSR_ARCHITECTURE.md` §12–13 for the full list)
- `eleves.statut` (`actif`/`suspendu`) is a column, **not** a row move between tables — suspension is `UPDATE`, never `DELETE`, specifically to avoid cascading away a student's `paiements`/`presences`/`notes` history. `eleves_suspendus` is a lightweight metadata table (one row per *active* suspension), not the student record itself.
- `matricule` is generated once (`src/lib/matricule.ts`, format `[INITIALE][MM][AA][XXXX]`) and is immutable thereafter — no UI or Server Action may ever change it.
- Payment deletion requires password + motif verified server-side, and is restricted to `coordonnateur`/`comptable` — never `superviseur`, even though `superviseur` can record payments.
- `chef_site`/`secretaire` never see: contacts parents, financial stats, WhatsApp button, comptabilité, paramètres, fin d'année, Paiements > Enregistrer/Supprimés. **`secretaire` had the identical profile to `chef_site` from 2026-08-15 to 2026-08-16** — since 2026-08-16 (client-requested adjustments, see "Historique et points ouverts") three points now differ: `chef_site` alone gets read-only access to Paiements > Historique/À jour/En retard (no action buttons, no export — `secretaire` still has zero Paiements access); `chef_site` alone sees the Dashboard's "Retard de paiement" alert panel (both see the "Non à jour" KPI); both now see Élèves suspendus (view-only, no Réinscrire/Exporter), Notifications (RLS-scoped by site), and Galerie admin (upload allowed, delete limited to their own uploads).
- Money amounts are FCFA integers; timezone is `Africa/Porto-Novo`.

### UI conventions
- Interface is French-only (labels, messages, routes) — technical tokens like `MoMo` or subject codes are the only exception.
- **Lexend is the single font sitewide** (admin, TD, portail parents, vitrine) — loaded once in `src/app/layout.tsx` (`--font-lexend` → Tailwind `font-sans`). Decided 2026-08-15. Before that: Inter sitewide by default with a vitrine-only Lexend override — and this doc used to claim a third, system-font treatment for admin/TD that never matched any actual code (caught by audit 2026-08-14).
- Brand green `#12AA00` / `#0e8f00` gradient on primary buttons, page headers, and the admin sidebar; `#f6b40a` accent is vitrine-only.
- Recurring admin patterns to follow when adding pages: gradient `PageHeader`, `ActionsBar` (icon+label buttons, label hidden on mobile), responsive `DataTable` (cards on mobile via `data-label`), `EmptyState`, colored pill `Badge`s — all already implemented in `src/components/admin/`.

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
